import React from 'react';

export default function About({ params }) {
    return(
        <section id="about">
            <div className="">
                Hectors Tree Service
            </div>
        </section>
    );
    
}